spring:
  datasource:
    url: jdbc:mysql://localhost:3306/liastudio_db
    username: liastudio_user
    password: liastudio123
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8081